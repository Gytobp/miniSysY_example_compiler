[jobs.lab3]
image = { source = "dockerfile", path = "."}

run = [
  #  "ls",
  #  "ls ..",
  "cat $input",
  #  "in=$(cat $input)",
  "java -jar ./miniSysY-Compiler-1.0-SNAPSHOT-jar-with-dependencies.jar $input > $ir",
]

[jobs.lab4]
image = { source = "dockerfile", path = "."}

run = [
#  "ls",
#  "ls ..",
  "cat $input",
#  "in=$(cat $input)",
  "java -jar ./miniSysY-Compiler-1.0-SNAPSHOT-jar-with-dependencies.jar $input > $ir",
]

[jobs.lab5]
image = { source = "dockerfile", path = "."}

run = [
  #  "ls",
  #  "ls ..",
  "cat $input",
  #  "in=$(cat $input)",
  "java -jar ./miniSysY-Compiler-1.0-SNAPSHOT-jar-with-dependencies.jar $input > $ir",
]

[jobs.lab6]
image = { source = "dockerfile", path = "."}

run = [
  #  "ls",
  #  "ls ..",
  "cat $input",
  #  "in=$(cat $input)",
  "java -jar ./miniSysY-Compiler-1.0-SNAPSHOT-jar-with-dependencies.jar $input > $ir",
]

[jobs.lab7]
image = { source = "dockerfile", path = "."}

run = [
  #  "ls",
  #  "ls ..",
  "cat $input",
  #  "in=$(cat $input)",
  "java -jar ./miniSysY-Compiler-1.0-SNAPSHOT-jar-with-dependencies.jar $input > $ir",
]

[jobs.lab8]
image = { source = "dockerfile", path = "."}

run = [
  #  "ls",
  #  "ls ..",
  "cat $input",
  #  "in=$(cat $input)",
  "java -jar ./miniSysY-Compiler-1.0-SNAPSHOT-jar-with-dependencies.jar $input > $ir",
]

[jobs.mem2reg]
image = { source = "dockerfile", path = "."}

run = [
  #  "ls",
  #  "ls ..",
  "cat $input",
  #  "in=$(cat $input)",
  "java -jar ./miniSysY-Compiler-1.0-SNAPSHOT-jar-with-dependencies.jar $input > $ir",
]
